# Krammy.xyz

Standalone Vite + React site.

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL shown by Vite.

## Customize

Update `src/member.ts` to change the Discord ID, profile fallback data, YouTube Music track, friend links, and social links.